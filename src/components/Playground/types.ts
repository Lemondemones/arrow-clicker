export type TArrows = {
  ArrowUp: string;
  ArrowDown: string;
  ArrowLeft: string;
  ArrowRight: string;
};

export type TEndGameCondition = {
  SUCCESS_COUNT: number;
  UNSUCCESS_COUNT: number;
};
